spring.datasource.url=jdbc:h2:mem:geardb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secret (in-memory key used by JwtUtil) - for production use env var or keystore
app.jwt.secret=replace-me
spring.application.name=demo
